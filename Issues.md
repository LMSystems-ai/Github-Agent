1. The loop between the react agent, aider node, and critique node has to be improved. The graph rarely ever routes back to the react node after modifying a graph currently.

2. The router nodes have to be improved.

3. It'd be nice to have a research subgraph with a perplexity "pro" sort of CoT to think through queries and return research analysis about it.